ALTER TABLE product
    ADD code varchar (8),
    ADD barCode varchar (12),
    ADD series varchar (5),
    ADD manufacturingDate date,
    ADD expirationDate date,
    ADD description varchar(200),
    ADD color varchar (25),
    ADD material varchar (25)
